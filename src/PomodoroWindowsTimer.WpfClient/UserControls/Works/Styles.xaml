<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls.Works"
                    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                    >
    <Style x:Key="st_WorkCardWrapperBorder"
           TargetType="Border"
           >
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="BorderThickness" Value="1,0,0,0" />
        <Setter Property="Background" Value="#030C10" />
        <Setter Property="BorderBrush" Value="{StaticResource MaterialDesign.Brush.Foreground}" />
        <Setter Property="TextElement.Foreground" Value="{StaticResource MaterialDesign.Brush.Primary}" />
    </Style>

    <Style x:Key="st_WorkEditButton"
           BasedOn="{StaticResource MaterialDesignFlatDarkBgButton}"
           TargetType="Button"
           >
        <Setter Property="ToolTip" Value="Edit" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="1,0,0,0" />
        <Setter Property="Foreground" Value="{StaticResource MaterialDesign.Brush.Primary.Light}" />
    </Style>
</ResourceDictionary>