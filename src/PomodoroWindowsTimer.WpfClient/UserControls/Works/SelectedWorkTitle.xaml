<UserControl x:Class="PomodoroWindowsTimer.WpfClient.UserControls.Works.SelectedWorkTitle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls.Works"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             Height="Auto"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d"
             >
    <materialDesign:ColorZone materialDesign:ElevationAssist.Elevation="Dp0"
                              Mode="Custom"
                              >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" SharedSizeGroup="PopupGroup" />
            </Grid.ColumnDefinitions>

            <!--  title: SELECTED WORK  -->
            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Margin="0,16,0,10"
                       Foreground="{StaticResource MaterialDesign.Brush.Primary.Dark}"
                       Text="SELECTED WORK"
                       TextAlignment="Center"
                       />
            <!--  selected work popup menu  -->
            <materialDesign:PopupBox Grid.Row="0"
                                     Grid.Column="1"
                                     Margin="0,0,3,0"
                                     Padding="0"
                                     Foreground="{StaticResource MaterialDesign.Brush.Foreground}"
                                     PlacementMode="BottomAndAlignRightEdges"
                                     StaysOpen="False"
                                     >
                <StackPanel Orientation="Vertical">
                    <Button Command="{Binding CreateWorkCommand}"
                            Content="Create New"
                            />
                    <Button Command="{Binding UnselectWorkCommand}"
                            Content="Unselect"
                            />
                </StackPanel>
            </materialDesign:PopupBox>

            <!--  selected work  -->
            <local:SelectedWork Grid.Row="1"
                                Grid.ColumnSpan="2"
                                Margin="0,10,0,0"
                                Padding="0,0,18,0"
                                VerticalAlignment="Center"
                                DataContext="{Binding SelectedWorkModel}"
                                IsSelected="True"
                                />
        </Grid>
    </materialDesign:ColorZone>
</UserControl>
