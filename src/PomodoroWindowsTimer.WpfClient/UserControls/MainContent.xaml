<UserControl x:Class="PomodoroWindowsTimer.WpfClient.UserControls.MainContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:MainModel="clr-namespace:PomodoroWindowsTimer.ElmishApp.MainModel;assembly=PomodoroWindowsTimer.ElmishApp"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:digitPanel="clr-namespace:p1eXu5.Wpf.DigitPanel;assembly=p1eXu5.Wpf.DigitPanel"
             xmlns:local="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls"
             xmlns:mainScreen="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls.MainScreen"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sel="clr-namespace:PomodoroWindowsTimer.WpfClient.Selectors"
             xmlns:tpDrawer="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls.TimePointsDrawer"
             xmlns:works="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls.Works"
             x:Name="m_MainContentRoot"
             d:DataContext="{d:DesignInstance Type=MainModel:IBindings,
                                              IsDesignTimeCreatable=False}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d"
             >
    <UserControl.Resources>
        <sel:WorkSubModelTemplateSelector x:Key="sel_WorkSubModelTemplate" />
        <local:DrawerWidthConverter x:Key="conv_DrawerWidth" />
    </UserControl.Resources>

    <materialDesign:DrawerHost x:Name="DrawerHost"
                               HorizontalAlignment="Stretch"
                               VerticalAlignment="Stretch"
                               BorderThickness="0"
                               IsLeftDrawerOpen="{Binding IsTimePointsDrawerShown}"
                               IsRightDrawerOpen="{Binding IsWorkSelectorLoaded}"
                               LeftDrawerBackground="Transparent"
                               LeftDrawerCloseOnClickAway="True"
                               OpenMode="Standard"
                               RightDrawerBackground="Transparent"
                               RightDrawerCloseOnClickAway="True"
                               >

        <!--  TimePointsDrawer  -->
        <materialDesign:DrawerHost.LeftDrawerContent>
            <tpDrawer:TimePointsDrawer Width="350"
                                       DataContext="{Binding}"
                                       />
        </materialDesign:DrawerHost.LeftDrawerContent>

        <!--  WorksDrawer  -->
        <materialDesign:DrawerHost.RightDrawerContent>
            <works:WorksDrawer Width="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=UserControl}, Path=ActualWidth, Converter={StaticResource conv_DrawerWidth}, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                               DataContext="{Binding WorkSelector}"
                               />
        </materialDesign:DrawerHost.RightDrawerContent>

        <!--  menu & MainScreen  -->
        <DockPanel Margin="10,0,10,0"
                   LastChildFill="True"
                   >
            <local:MainMenu DockPanel.Dock="Top"
                            DataContext="{Binding}"
                            />

            <materialDesign:ColorZone x:Name="m_ColorZone"
                                      HorizontalAlignment="Stretch"
                                      VerticalAlignment="Stretch"
                                      materialDesign:ThemeAssist.Theme="Inherit"
                                      Background="Transparent"
                                      >
                <Grid HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="5*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="6*" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <!--  Digit Panel  -->
                    <mainScreen:MainScreen x:Name="m_DigitPanel"
                                           Grid.Row="1"
                                           Grid.Column="1"
                                           DataContext="{Binding}"
                                           DigitBackground="{DynamicResource MaterialDesign.Brush.Secondary.Dark}"
                                           Foreground="{DynamicResource MaterialDesign.Brush.Secondary}"
                                           />
                </Grid>

            </materialDesign:ColorZone>
        </DockPanel>
    </materialDesign:DrawerHost>
</UserControl>
