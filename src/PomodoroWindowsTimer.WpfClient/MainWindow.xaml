<Window x:Class="PomodoroWindowsTimer.WpfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:PomodoroWindowsTimer.WpfClient.CustomControls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:digitPanel="clr-namespace:p1eXu5.Wpf.DigitPanel;assembly=p1eXu5.Wpf.DigitPanel"
        xmlns:local="clr-namespace:PomodoroWindowsTimer.WpfClient"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:settings="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls.Settings"
        xmlns:usercontrols="clr-namespace:PomodoroWindowsTimer.WpfClient.UserControls"
        xmlns:vm="clr-namespace:PomodoroWindowsTimer.ElmishApp;assembly=PomodoroWindowsTimer.ElmishApp"
        Title="{Binding Title}"
        Width="800"
        Height="520"
        MinWidth="650"
        MinHeight="420"
        Background="{DynamicResource {x:Static digitPanel:DigitPanelColors.DigitPanelBackgroundKey}}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Icon="pomodoro-technique.ico"
        TextElement.FontSize="13"
        TextElement.FontWeight="Regular"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        WindowStartupLocation="CenterScreen"
        mc:Ignorable="d"
        >
    <Window.Resources>
        <DataTemplate x:Key="dt_BotSettingsDialog">
            <settings:BotSettings DataContext="{Binding}" />
        </DataTemplate>
    </Window.Resources>

    <materialDesign:DialogHost DialogClosing="DialogHost_DialogClosing"
                               DialogContentTemplate="{StaticResource dt_BotSettingsDialog}"
                               DialogTheme="Inherit"
                               Identifier="RootDialog"
                               SnackbarMessageQueue="{Binding ElementName=m_ErrorMessageQueueSnackbar,
                                                              Path=MessageQueue}"
                               >

        <materialDesign:DrawerHost x:Name="DrawerHost"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   BorderBrush="{DynamicResource MaterialDesign.Brush.TextBox.HoverBackground}"
                                   BorderThickness="2"
                                   BottomDrawerBackground="{DynamicResource MaterialDesign.Brush.Secondary.Light}"
                                   BottomDrawerCornerRadius="20 20 0 0"
                                   >
            <materialDesign:DrawerHost.LeftDrawerContent>
                <usercontrols:RunningTimePoints Width="350"
                                                DataContext="{Binding}"
                                                />
            </materialDesign:DrawerHost.LeftDrawerContent>

            <!--  time points generator  -->
            <materialDesign:DrawerHost.RightDrawerContent>
                <usercontrols:TimePointsGenerator DataContext="{Binding TimePointsGeneratorModel}" />
            </materialDesign:DrawerHost.RightDrawerContent>

            <DockPanel Margin="10,0,10,0"
                       LastChildFill="True"
                       >
                <usercontrols:MainMenu DockPanel.Dock="Top"
                                       DataContext="{Binding}"
                                       />
                <materialDesign:Snackbar x:Name="m_ErrorMessageQueueSnackbar"
                                         DockPanel.Dock="Bottom"
                                         HorizontalAlignment="Stretch"
                                         ActionButtonStyle="{StaticResource MaterialDesignSnackbarActionDarkButton}"
                                         MessageQueue="{Binding ErrorMessageQueue}"
                                         />

                <materialDesign:ColorZone VerticalAlignment="Center"
                                          Background="{DynamicResource {x:Static digitPanel:DigitPanelColors.DigitPanelBackgroundKey}}"
                                          >
                    <!--  Content  -->
                    <DockPanel Margin="0,0,0,20"
                               LastChildFill="True"
                               >
                        <DockPanel.Resources>
                            <DataTemplate x:Key="gp_Green">
                                <StackPanel HorizontalAlignment="Center"
                                            Orientation="Vertical"
                                            >
                                    <TextBlock Margin="0,0,0,20"
                                               HorizontalAlignment="Left"
                                               Foreground="#00FF00"
                                               Text="{Binding DataContext.ActiveTimePointName,
                                                              RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                             AncestorType=Control}}"
                                               />
                                    <digitPanel:TimerTable HorizontalAlignment="Stretch"
                                                           VerticalAlignment="Center"
                                                           DigitBackground="#004000"
                                                           Foreground="#00FF00"
                                                           Format="mm':'ss"
                                                           Value="{Binding DataContext.ActiveTime,
                                                                           RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                                          AncestorType=Control},
                                                                           FallbackValue=00:00:00.00}"
                                                           />
                                    <Slider DockPanel.Dock="Top"
                                            Margin="0,40,0,0"
                                            Foreground="#00FF00"
                                            IsEnabled="{Binding DataContext.IsActiveTimePointSet,
                                                                RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                               AncestorType=Control}}"
                                            Maximum="{Binding DataContext.ActiveTimeDuration,
                                                              RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                             AncestorType=Control}}"
                                            Minimum="0"
                                            Orientation="Horizontal"
                                            PreviewMouseDown="Slider_MouseDown"
                                            PreviewMouseUp="Slider_MouseUp"
                                            Value="{Binding DataContext.ActiveTimeSeconds,
                                                            RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                           AncestorType=Control}}"
                                            />
                                </StackPanel>
                            </DataTemplate>

                            <DataTemplate x:Key="gp_Red">
                                <StackPanel HorizontalAlignment="Center"
                                            Orientation="Vertical"
                                            >
                                    <TextBlock Margin="0,0,0,20"
                                               HorizontalAlignment="Left"
                                               Foreground="#FF0000"
                                               Text="{Binding DataContext.ActiveTimePointName,
                                                              RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                             AncestorType=Control}}"
                                               />
                                    <digitPanel:TimerTable HorizontalAlignment="Stretch"
                                                           VerticalAlignment="Center"
                                                           DigitBackground="#400000"
                                                           Foreground="#FF0000"
                                                           Format="mm':'ss"
                                                           Value="{Binding DataContext.ActiveTime,
                                                                           RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                                          AncestorType=Control},
                                                                           FallbackValue=00:00:00.00}"
                                                           />
                                    <Slider DockPanel.Dock="Top"
                                            Margin="0,40,0,0"
                                            Foreground="#FF0000"
                                            IsEnabled="{Binding DataContext.IsActiveTimePointSet,
                                                                RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                               AncestorType=Control}}"
                                            Maximum="{Binding DataContext.ActiveTimeDuration,
                                                              RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                             AncestorType=Control}}"
                                            Minimum="0"
                                            Orientation="Horizontal"
                                            PreviewMouseDown="Slider_MouseDown"
                                            PreviewMouseUp="Slider_MouseUp"
                                            Value="{Binding DataContext.ActiveTimeSeconds,
                                                            RelativeSource={RelativeSource Mode=FindAncestor,
                                                                                           AncestorType=Control}}"
                                            />
                                </StackPanel>
                            </DataTemplate>

                            <local:DigitPanelTemplateSelector x:Key="dts_DigitPanel" />
                        </DockPanel.Resources>

                        <!--  Digit Panel  -->
                        <ContentControl x:Name="m_DigitPanel"
                                        DockPanel.Dock="Top"
                                        HorizontalAlignment="Center"
                                        Content="{Binding IsBreak}"
                                        ContentTemplateSelector="{StaticResource dts_DigitPanel}"
                                        DataContext="{Binding}"
                                        />

                        <usercontrols:ControlButtons DataContext="{Binding}" />

                    </DockPanel>
                </materialDesign:ColorZone>
            </DockPanel>
        </materialDesign:DrawerHost>
    </materialDesign:DialogHost>
</Window>
