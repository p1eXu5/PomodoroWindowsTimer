<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
                    xmlns:local="clr-namespace:PomodoroWindowsTimer.Wpf"
                    >
    <Style TargetType="{x:Type local:GlossOnBase}">
        <Setter Property="Width" Value="Auto" />
        <Setter Property="Height" Value="Auto" />
        <Setter Property="BaseFill" Value="{DynamicResource {x:Static local:PwtBrushes.BaseTextureKey}}" />
        <Setter Property="GlossFill" Value="{DynamicResource {x:Static local:PwtBrushes.GlossTextureKey}}" />
        <Setter Property="BeamOpacity" Value="0.25" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:GlossOnBase}">
                    <Grid Width="{TemplateBinding Width}"
                          Height="{TemplateBinding Height}"
                          HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                          Background="{TemplateBinding BaseFill}"
                          >
                        <!--  Gloss Rectangle  -->
                        <Rectangle Fill="{TemplateBinding GlossFill}"
                                   OpacityMask="{TemplateBinding GlossOpacityMask}"
                                   />

                        <!--  LCD  -->
                        <ContentPresenter Margin="{TemplateBinding Control.Padding}"
                                          Content="{TemplateBinding ContentControl.Content}"
                                          ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}"
                                          ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                          />

                        <!--  Beam Rectangle  -->
                        <Rectangle Fill="{TemplateBinding BeamFill}"
                                   IsHitTestVisible="False"
                                   Opacity="{TemplateBinding BeamOpacity}"
                                   OpacityMask="{TemplateBinding GlossOpacityMask}"
                                   Visibility="{TemplateBinding BeamVisibility}"
                                   />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>